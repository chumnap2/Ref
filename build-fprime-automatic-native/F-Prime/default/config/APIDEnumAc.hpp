// ======================================================================
// \title  APIDEnumAc.hpp
// \author Generated by fpp-to-cpp
// \brief  hpp file for APID enum
// ======================================================================

#ifndef ComCfg_APIDEnumAc_HPP
#define ComCfg_APIDEnumAc_HPP

#include "Fw/FPrimeBasicTypes.hpp"
#include "Fw/Types/Serializable.hpp"
#include "Fw/Types/String.hpp"

namespace ComCfg {

  //! APIDs are 11 bits in the Space Packet protocol, so we use U16. Max value 7FF
  class APID :
    public Fw::Serializable
  {

    public:

      // ----------------------------------------------------------------------
      // Types
      // ----------------------------------------------------------------------

      //! The serial representation type
      typedef U16 SerialType;

      //! The raw enum type
      enum T {
        //! Command packet type - incoming
        FW_PACKET_COMMAND = 0,
        //! Telemetry packet type - outgoing
        FW_PACKET_TELEM = 1,
        //! Log type - outgoing
        FW_PACKET_LOG = 2,
        //! File type - incoming and outgoing
        FW_PACKET_FILE = 3,
        //! Packetized telemetry packet type
        FW_PACKET_PACKETIZED_TLM = 4,
        //! Data Product packet type
        FW_PACKET_DP = 5,
        //! F Prime idle
        FW_PACKET_IDLE = 6,
        //! F Prime handshake
        FW_PACKET_HAND = 254,
        //! F Prime unknown packet
        FW_PACKET_UNKNOWN = 255,
        //! Per Space Packet Standard, all 1s (11bits) is reserved for Idle Packets
        SPP_IDLE_PACKET = 2047,
        //! Anything equal or higher value is invalid and should not be used
        INVALID_UNINITIALIZED = 2048,
      };

      //! For backwards compatibility
      typedef T t;

    public:

      // ----------------------------------------------------------------------
      // Constants
      // ----------------------------------------------------------------------

      enum {
        //! The size of the serial representation
        SERIALIZED_SIZE = sizeof(SerialType),
        //! The number of enumerated constants
        NUM_CONSTANTS = 11,
      };

    public:

      // ----------------------------------------------------------------------
      // Constructors
      // ----------------------------------------------------------------------

      //! Constructor (default value of INVALID_UNINITIALIZED)
      APID()
      {
        this->e = INVALID_UNINITIALIZED;
      }

      //! Constructor (user-provided value)
      APID(
          const T e1 //!< The raw enum value
      )
      {
        this->e = e1;
      }

      //! Copy constructor
      APID(
          const APID& obj //!< The source object
      )
      {
        this->e = obj.e;
      }

    public:

      // ----------------------------------------------------------------------
      // Operators
      // ----------------------------------------------------------------------

      //! Copy assignment operator (object)
      APID& operator=(
          const APID& obj //!< The source object
      );

      //! Copy assignment operator (raw enum)
      APID& operator=(
          T e1 //!< The enum value
      );

      //! Conversion operator
      operator T() const
      {
        return this->e;
      }

      //! Equality operator
      bool operator==(T e1) const
      {
        return this->e == e1;
      }

      //! Inequality operator
      bool operator!=(T e1) const
      {
        return !(*this == e1);
      }

#ifdef BUILD_UT

      //! Ostream operator
      friend std::ostream& operator<<(
          std::ostream& os, //!< The ostream
          const APID& obj //!< The object
      );

#endif

    public:

      // ----------------------------------------------------------------------
      // Member functions
      // ----------------------------------------------------------------------

      //! Check raw enum value for validity
      bool isValid() const;

      //! Serialize raw enum value to SerialType
      Fw::SerializeStatus serializeTo(
          Fw::SerializeBufferBase& buffer //!< The serial buffer
      ) const;

      //! Deserialize raw enum value from SerialType
      Fw::SerializeStatus deserializeFrom(
          Fw::SerializeBufferBase& buffer //!< The serial buffer
      );

#if FW_SERIALIZABLE_TO_STRING

      //! Convert enum to string
      void toString(
          Fw::StringBase& sb //!< The StringBase object to hold the result
      ) const;

#endif

    public:

      // ----------------------------------------------------------------------
      // Member variables
      // ----------------------------------------------------------------------

      //! The raw enum value
      T e;

  };

}

#endif
